# MR Memory

MR 为 AstrBot 提供群聊语义背景：记录群友发言、引用、机器人实际回答与工具活动，让潜意识模型主动检索相关经历，再交给主回复模型自然参与对话。网络搜索、绘图等外部能力由 AstrBot 和其他插件提供。

默认持续整理群聊，建立带原文来源的事件、语义记忆和关联，供回答前主动检索。高级反思、持续注意和独立反馈学习保留为实验功能，默认关闭。基础建图不依赖这些实验功能；原文索引也不能代替模型整理。模型能运行工具或完成写入，并不代表已经可靠理解反讽、人物归属和复杂社交关系。

## 开始使用

在 AstrBot 插件配置中启用 `capture_enabled`，选择需要记录的群，设置 `subconscious_provider_id`。`local_serving_reader_provider_id` 留空时使用同一个 provider，也可以单独指定回答前检索模型。插件不改变 AstrBot 的主回复模型。

| 配置 | 作用与默认值 |
| --- | --- |
| `capture_enabled` | 记录群聊，默认关闭，使用前开启 |
| `local_serving_enabled` | 回答前检索，默认开启 |
| `member_prefix_capacity` | 固定前缀最多携带的活跃成员数，默认 50；0 关闭该前缀 |
| `local_serving_timeout_seconds` / `max_loop_steps` | 检索异常超时 180 秒、最多 6 轮；模型可提前结束 |
| `advanced_memory_enabled` | 高级记忆实验总开关，默认关闭 |
| `subconscious_enabled` / `auto_distillation_enabled` | 后台工作、自动整理，默认开启，基础模式也持续建图 |
| `feedback_learning_enabled` | 反馈学习，默认关闭，仅高级模式可用 |

已有 provider、上下文、向量模型和资源设置继续有效。旧公开配置优先，更名字段由 `mr_memory/settings.py` 兼容。高级开关只控制实验性能力；基础整理与回答前检索各自运行。

启用消息记录后，OneBot 群聊默认支持断线历史回补。插件启动、NapCat 重连时自动补查，并每五分钟核对一次；首次检查近 24 小时，可在配置中调整。之后按独立保存的核对进度分页续接，即使新消息已经恢复接收，也不会跳过先前缺口。

回补只把缺失的消息写入原文库，按群和消息 ID 去重，保留原作者、时间、引用及媒体引用；不触发旧命令或重发回答，也不额外调用 LLM。补回内容由现有索引和后台整理继续处理。前端“断线历史回补”显示累计补入数量、进度和平台无法提供的范围，也可手动补查近期历史。QQ/NapCat 无法返回的内容不会显示为已补齐。

## 群员资料与称呼

完整资料库保存所有已观察或由平台成员列表获得的群员。UID 对应真实平台账号，群名片、平台昵称、历史显示名分别保留。同名不会合并不同 UID，换名不会新建一个人；机器人账号和昵称里恰好包含 bot 的群友也分别记录。

活跃前缀只是资料库的一部分：服务当地每天首次使用时，按最近七天发言量选出最多 50 人，按 UID 排序，当天保持名单及资料快照稳定。第二天若所选人和资料没有变化，前缀文字也不变。本人或管理员主动修改资料立即生效，不必等到次日。当前发言者、被引用者和提及账号的最新资料另行提供，即使没有进入前缀；其他成员仍可由模型按 UID 或称呼查询。

**前缀缓存不是免费记忆**：命中仍有输入费用，仍占模型上下文；首次调用、资料变化或供应商缓存失效可能按普通输入计费。人数越多、每人说明越长，成本与上下文占用也越大。MR 控制前缀稳定性，实际命中以供应商 usage 为准。

群友发送 `/mr uid` 查看自己的 UID、资料和编辑说明，例如：

```text
/mr uid 称呼 小林
/mr uid 称呼 小林 别名 林同学,小林同学
/mr uid 别名 小林,林同学
/mr uid 不用 旧外号
/mr uid 说明 平时聊摄影，叫小林就好
/mr uid 别名 -
```

可以在一条命令里连写多个字段，整条解析完后一次保存；`-` 清空对应字段。多个别名用逗号分隔；字段值含有“称呼”“别名”等参数名时，用英文引号包住，例如 `/mr uid 说明 "别名 只是说明里的一个词"`。缺少字段内容或引号未闭合时会提示，不会保存前半条命令。命令始终操作发命令者本人，不能指定另一个 UID。本人说明保留自述性质，不会认证其中涉及的经历或他人信息。

管理员在控制台“账户与别名”页选择 UID，可编辑任何已记录群员的称呼、别名、不使用的称呼和说明；群名片与平台昵称由平台同步，不被手动资料覆盖。页面展示实际活跃前缀及其字符数。明确的本人／管理员称呼优先于旧摘要里的叫法，模型仍须结合语境理解一句话在说谁。

## 检索怎样运行

潜意识模型得到当前请求、带作者和引用的近期交流，以及与主模型一致的成员资料。它自行决定检索词、语义查询、图导航、要展开的上下文和结束时机。可检索原始消息、记忆、成员及活动，按需读取主模型当前输入与工具目录。基础模式中，回答前只读检索，后台另行整理和写入。

原文区分群友发言、bot 生成稿、实际发送和工具调用。引文保留原作者，搜索可区分某人发言与谈及某人；同一次生成和发送不算两份独立证据。相同原文在同轮内用引用复用，发生变化的内容正常提供。找不到锚点、参数错误、请求失败与有效空结果分别呈现。

新的人类发言及 bot 回答进入本地向量索引，无须先支付模型整理费用。旧数据的向量逐步补齐，尚未建立向量的原文仍可用词法搜索。旧模型摘要保留为检索入口，并标明是解释；重复检索不会自动强化它。

交付只补充当前缺少的群内来历，不替主模型执行外部行动。主意识继续使用自己的工具、人格与对话历史。MR 文字视图里的图片地址不等于它已经看到图片，主意识的实际媒体能力由自身输入决定。

## 记忆图怎样更新

后台模型读取尚未整理的群聊，按需查阅既有记忆和原文，将具体事件、自述、事物及相互联系存入同一张可修订的图。每项认识保留作者语境、原文入口和把握程度；模型选择检索线索与连接，不用关键词规则代替语义理解。明确纠正可以修订有关记忆，即使独立反馈学习没有开启。

默认在**服务当地时间** 00:00–06:00 自动整理，使用后台整理的滚动 24 小时 Token 额度；回答前检索即时、独立计费。基础整理也得到稳定的成员前缀及本批涉及账号的最新资料。未处理材料保留在队列，原文仍可先行查询。向量索引独立排队，慢索引不阻塞整理调度；新记忆保存后先能通过原文、关键词与图访问，向量完成后再进入语义相似度查询。手动“整理新消息”可以立即运行；关闭后台工作才会停止新语义记忆建图。

## 控制台

在 AstrBot 中打开 MR 插件页面 `console`，沿用 AstrBot 管理登录。

- **运行与调用**：查看当前模式、用量、耗时和调用的状态变化、工具过程、背景交付与实际回复。缺失的旧过程不会补造。
- **记忆与连接**：查找持续整理形成的事件、语义关联和原始依据。
- **账户与别名**：完整成员目录、活跃前缀及管理员编辑入口。
- **原始消息**：查看作者、时间、引用和连续上下文。
- **清理主会话上下文**：主模型持续异常时可手动新建当前群会话。旧会话、人格、MR 数据保留；不发群消息，不重启服务。清理本身不证明故障已经恢复。

“待整理消息”表示尚未完成模型整理的原文，不是已经完成的向量索引数量；不会假报已整理来清零。

## 高级实验与开发

高级模式增加持续注意、理解经验回流、跨经历抽象连接和后续反思，需要显式开启总开关及相应后台开关。反馈使用独立的滚动 24 小时 Token 额度。切换到基础模式时，旧实验任务和草稿暂停保存，不会挡住基础整理的新批次；已经形成的记忆及来源仍可检索。

当前职责和实验边界见 [架构说明](docs/memory-redesign.md)。模型能力不足时，外部图结构和不确定性字段不能保证正确的社交理解；默认基础模式是当前产品取舍。

`main.py` 接入 AstrBot；`mr_memory/profiles.py` 管理资料和稳定前缀；`store.py` 管理原文与存储；`agent.py` 和 `memory_protocol.py` 管理模型工具；`pages/console/` 提供管理页面。

真实语料、评测题目、逐题回答和人工审核只保存在仓库外的本地私有目录；仓库测试使用合成内容。真实理解评测只有最终分数可公开，未核查的助理初答不是标准答案或通过证明。

通过插件热重载更新，卸载时释放接口、观察器和在途任务，无须重启 AstrBot 或 NapCat。
