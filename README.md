# MR Memory

MR 为 AstrBot 提供基于群聊经历的语义背景。它保存群友发言、机器人回应与可观察的工具活动，由原生 DeepSeek V4 Flash 主动查找相关事件、人物和关系，再把理解交给 AstrBot 的主回复模型。网络搜索、绘图等外部能力由其他插件负责。

开发分支 `codex/mr-memory-redesign` 正在准备记忆架构重构：[目标、图结构与替换边界](docs/memory-redesign.md)。该方案尚未实现；下文继续描述现有运行版本。

## 工作方式

当前发言者和近期对话进入潜意识模型上下文。模型理解语境后，自行选择词法或语义搜索，查原始消息、连续上下文、事件、人物、关系图和活动统计，根据读到的材料继续搜索或结束。结果是自然语言背景；没有前置语义审批、逐事实证书或第二套审查模型。潜意识在同一次输出中分别生成本轮背景与跨轮短期笔记：前者交给主模型，后者保留正在延续的理解与纠正，供下一次潜意识自行修改。换话题不会自动清掉笔记；旧笔记仍是可修订的理解，不作为新的原始事实。

后台从群聊原文形成事件、语义与关联记忆。反馈学习是独立的模型调用：将机器人原回答与后续交流放在一起，由模型理解纠正、评价及群内含义，并把形成的认识提供给后续回忆。消息整理与反馈学习分别启用、分别计费；不是靠关键词判断用户是否满意。工作记忆随调用更新，长期记忆留在 SQLite；未完成的构建和索引工作仍可继续处理。

模型直接阅读自然称呼、实际账号和带作者的原始消息，不再使用临时人物代号。搜索返回旧记忆摘要与来源目录，模型可以打开其中的完整原文、展开上下文理解指代。同一轮上下文内重复命中的相同原文只传一次，保留引用地址。原文检索区分“某账号发出的消息”和“提及、回复涉及某账号的消息”。引用时间取本群数据库里的原消息时间，未保存原消息时明确时间未知。

搜索接口接受数字或字符串形式的同一真实账号，统一按账号文本查询。词法搜索的 `terms` 每项按完整子串匹配，多项为 OR，空格不会自动拆词；按含义寻找不同措辞时由模型选择 `semantic_search(query)`。原文词法搜索默认带出每个命中的下一条交流，让紧随其后的回应也能被理解；模型可调整 `before/after` 或继续展开。作者与角色条件只限制命中，邻接可来自其他人，所有内容遵守本次请求的时间截止，重叠原文只给一次。邻接本身不代表纠正或赞同，由模型理解。参数用错时返回具体字段、所需类型和当前工具说明，让模型自行纠正，不自动改写查询或切换搜索算法。上下文查询区分 MR 内部编号与平台消息号；找不到锚点时明确报告地址问题，不把查址失败作为正常空上下文返回给模型。

展开前后文时，已经给过的相同正文用 `source_ref` 指向当前会话中先前提供的字段，仍保留本条作者、时间和排列顺序；新字段与编辑后的原话照常传入。并行读取按实际交付顺序生成引用，避免引用尚未出现的原文。记忆及关系图目录保留反思的认识、状态、优先级与来源地址，完整反思原文由 `memory` 或 `reflect(id)` 打开。历史工具结果的正文仍可完整阅读，不按工具类型或内容关键词裁剪。

工具活动和机器人回应带着其关联的原请求与作者，即使原请求落在近期消息窗口之外也能看见归属。本次 AstrBot 请求的实际管理员身份由事件直接提供给潜意识和主模型，与 QQ 群角色分开；这个事实不代表所有工具都已授权或可用。图片等附件的 Base64 编码保留在原始记录中，不参与词语搜索，也不作为自然语言输入；文本视图保留附件提示、原消息地址和可用的媒体 URL。

后台也能主动检索和修订旧理解。`remember` 返回已保存的记忆与图节点地址，让模型继续复用节点或修改连接；同名节点不会自动合并，昵称和关系由模型结合对话理解。模型明确完成的部分立即记录进度，中途已保存的记忆和后续思路供下一轮继续使用。

发现错认、矛盾或后果值得重视的理解时，模型可用 `reflect` 留下具体疑点、关联记忆、原文与继续查阅的优先程度。反馈学习沿用已有额度处理这些关注，通过 `interaction` 回到实际请求、当时交付的背景、机器人回答与后续交流。已经查清的内容直接修订；仍缺线索的可以等待新交流或约定复访时间。关注在热重载后保留，也会随相关记忆与图节点一起被读到，无须每次回答另跑一轮审查。

修订会替换旧来源及受影响的图连接、更新索引，并保留旧理解和修改原因供后续反思；不再成立的认识可以撤回。检索原文时，模型还可自行选择群友发言、机器人回答或工具活动，默认仍包含全部类型。重新打开前后文会完整呈现连续交流；回看特定互动时也能找到当时认为已解决的旧反思，继续修订自己的认识。

后台使用原有时间与滚动 Token 额度，不套用回答前搜索的轮数限制。接近资源边界前提醒模型保存；按实际输入增长估算只够最后一次调用时，该次只提供原生 `remember` 保存工具，使模型先保存理解与进度。普通学习轮次保留完整检索与反思工具，模型决定哪些材料已理解，未完思路留在 checkpoint，下轮继续；不把尚未处理的材料标成完成。最后一次调用的工具表变化可能减少缓存命中，取舍是让模型通过原生工具格式完成写入。未解决的关注继续参与后续理解。

这种主动搜索沿用 [MRAgent 论文](https://arxiv.org/html/2606.06036v1) 的“根据中间证据调整下一步访问”机制。群聊接入、跨请求工作记忆和后台更新属于本插件的产品实现；不声称复现论文基准分数。

## 配置和数据

在 AstrBot 的插件配置面板设置 MR。`subconscious_provider_id` 指定后台记忆模型；`local_serving_reader_provider_id` 可单独指定回答前的记忆模型，留空沿用前者。主回复模型与网络搜索、绘图等工具仍由 AstrBot 自身配置。

旧版公开配置字段及其含义已恢复，显式配置优先，公共默认只填补缺失项。精简版本的 `memory_timeout_seconds`、`memory_max_turns`、`background_min_messages` 等更名字段会映射回原字段；同时存在时以原公开字段为准。下面是公共默认值，不代表已有实例的配置：

| 配置 | 用途与公共默认 |
| --- | --- |
| `capture_enabled` | 是否记录群聊经历，默认关闭；使用前按需要开启 |
| `local_serving_enabled` | 回答前主动检索并提供背景，默认开启，与后台开关独立 |
| `local_serving_timeout_seconds` / `max_loop_steps` | 异常超时 180 秒、最多 6 轮；模型读够即可结束，不要求用满 |
| `subconscious_enabled` | 后台整理与反馈学习的总开关，默认开启 |
| `auto_distillation_enabled` / `auto_distillation_min_pending` | 自动整理默认开启，累计 150 条触发 |
| `maintenance_interval_seconds` | 不足条数时最长等待 86400 秒；不是模型调用超时 |
| `distillation_max_messages` | 每批最多整理 80 条 |
| `learning_window_enabled` / `learning_window_start` / `learning_window_end` | 优先工作时段默认开启，按服务进程当地时间 00:00–06:00；支持跨午夜，关闭或起止相同表示全天 |
| `distillation_thinking_mode` / `feedback_thinking_mode` | 后台整理与反馈默认开启模型思考，回答前默认关闭思考 |
| `distillation_max_output_tokens` / `maintenance_llm_timeout_seconds` | 后台单次输出上限 384000 Token、异常超时 3600 秒；均不是目标消耗或耗时 |
| `feedback_learning_enabled` | 独立反馈学习默认关闭；开启后按反馈窗口与合并等待设置处理 |
| `private_daily_token_budget` / `feedback_daily_token_budget` | 后台整理与反馈分别为每群滚动 24 小时 50 万 Token，0 表示不限 |

两项后台额度按最近 24 小时累计，不在零点清空。已有旧账本及其人工重置边界继续参与各自统计；精简版本只留下自然日总量的部分以明确标记的估计结转，无法补造逐次时间。新调用先预留，再用返回的实际用量结算；失去响应时保留预留量。新版本的回答前搜索不占这两项后台额度。

回答前检索到达原有轮数或时间边界时，保留相同工具表，以供应商原生 `tool_choice="none"` 请求模型收尾，避免移除工具定义破坏可复用前缀。实际缓存命中仍取决于供应商返回的用量，不能由请求格式保证。

群聊记录和回答前搜索即时运行。普通后台整理、反馈深思在配置时段内开始；手动整理和模型明确预约到期的关注可以提前执行，已开始的调用会保存进度后结束。反馈关联时长按互动发生时间计算，排队不会让白天反馈到了晚上就消失。

后台任务固定本批材料，模型通过 `remember` 的 `progress` 明确标记已处理消息并保存后续思路；证据来源不等于处理完成。材料进度独立持久化，不因某条记忆草稿尚未保存而扣留整批完成标记。反馈按原始消息分别记录首次完成时间，续接不会重写已完成材料的日期；旧版连续水位仅作为已处理历史的边界保留，不会因为完成近期批次就跳过旧材料。输入按剩余额度组织整条消息，未提供的部分保留待办。普通轮次的工具表与既有会话前缀保持稳定；不足以继续完整会话时，可从模型保存的理解、实际记忆地址和按需打开的旧调用继续。

`remember` 分别保存每条草稿：一项缺来源或写入失败，不再阻止其他项落库。回执给出已保存地址和失败草稿的 `pending_id`；模型用 `retry` 只补需要修改的字段，也可说明原因放弃某条草稿。来源始终由模型选择，不用整批消息代填。草稿与保存回执随任务持久保存，单项写入和其回执同事务提交；未处理的草稿不会被另一条成功写入清掉。模型明确结束已完成材料批次时，待修草稿连同原调用与已保存记忆地址原子移交到独立待办，后续批次仍可处理；它不是必须重复核查的整批关卡。旧版本已暂存的明确完成标记在下次后台检查时落地，不需要再付费询问模型。

`remember` 和最终 JSON 保存的 checkpoint 都可用于缩短续接会话，保留产生该总结的模型输出及其之后的工具结果。输入预算估算保留此前供应商返回用量对应的 Token／字节比例，按新的输入体积估算；同一模型的后续批次也复用这个比例，下一次响应继续校准。保存空间还参考已经实际发生的最大单次输出用量（含思考），短搜索请求不会降低这份预留。新批次按输入及输出所需的空间选取材料；这些是调度估算，实际结算仍使用供应商返回的 Token 数。

读取结果会使下一次输入无力支付并保存时，先把完整结果持久保存在本次运行步骤，再给模型结果地址与“尚未读到”的明确说明，让它保存当前理解和未完线索。模型之后可用 `interaction(run_id, step)` 读取这份完整结果，不用重新检索；未确认存档成功的内容仍完整保留在会话中。这只发生在配置余额不足时，不按内容筛选记忆。

后台初始输入提供反思目录，保留模型写下的疑点、认识和来源地址；完整原文由 `reflect(id)` 按需打开。同时提供真实待整理总量，让模型知道额度还要供其他批次使用。成批原文共用表头与相同字段值，每行仍保留作者账号、称呼、角色、时间、正文、引用和附件；这个编码只用于后台，回答前的消息格式保留。同一会话再次读到内容完全相同的记忆字段时，以 `memory_ref` 指向此前的记忆，变化的内容仍完整提供；原文也沿用已有的 `source_ref`。正常续接保留原有前缀，仅在完整输入付不起时重新编码旧会话中的重复字段，不删除模型思考、工具调用或独有内容。

模型工具参数直接取供应商保留的原始返回。参数 JSON 出错时，原字符串与未执行状态进入后续上下文，让模型修正该次调用；不会把框架解析失败后给出的空对象当成成功保存。真正的空 `remember` 参数也会返回错误。DeepSeek 偶尔以最终文本返回的完整 DSML `remember` 调用也按其原生参数读取，仍走同一写入与进度路径。已正常结束的 JSON 如果仅漏掉最外层右括号，且加回一个括号即可完整解析原有所有值，会记录该格式补正后保存；不补造缺失字段或把截断输出当完整调用。其他格式错误会将失败回执交回模型，已成功的写入继续保留，无须重做。

最后一次 `remember` 可同时声明 `finish=true`：本批材料均已处理且模型明确结束时直接结束，不另付一次完整上下文的调用来确认结束；仍待修的草稿数量单独报告。材料进度到齐并不自动结束模型的反思；续接保留它保存的 checkpoint、相关记忆、未完成写入与 checkpoint 之后的工具结果。同群仍有效的原文地址可直接用于修订，不因本轮尚未重新打开而报地址错误。

“待整理”表示已入库但尚未完成后台理解整理，部分闲聊最终无需另存长期记忆，不表示回答前查不到原文。普通整理在完成当前批次后优先处理近期消息；反馈在近期批次与历史批次之间交替，只有实际完成一批才切换，未完成时继续其已保存思路。按发言时间组织交流，保留机器人原回答与其原请求，由模型判断交流含义。这个调度让近期反馈不必等历史队列全部清空，同时为旧材料保留处理机会；是否已经追上消息增长仍须看实际处理量，不能把修改队列等同于清空积压。

本地语义检索的公共默认模型是 `BAAI/bge-small-zh-v1.5`；已有实例显式配置的其他模型会保留。Harrier 可以继续使用，`embedding_query_prompt_name=auto` 会为它选择对应查询提示。首次使用需要下载所选公开模型并预热，之后使用本地缓存。关闭向量检索时，记忆模型仍可查询原始消息、成员和关系。源码仓库不包含群聊语料、配置凭据、模型文件或运行报告。

数据库沿用 `data/plugin_data/astrbot_plugin_mr_memory/scopes/`，每群独立存储，已有群库、原文和图数据保留。删除的旧处理器与证书流程不再参与运行。

`/mrmem` 查看本群最近一次潜意识运行状态。`/mrforget` 删除并停止记录自己的本群个人记忆。

## 控制台

从 AstrBot 的 MR 插件页面入口打开 `console`。控制台使用 AstrBot 已有登录与页面桥接，不需要另设账户：

“运行与调用”可以打开每次回忆、整理或反馈学习的执行轨迹：查看已发生的状态路径，逐步阅读模型普通输出、工具查询与返回、记忆写入、索引更新及背景交付。运行中自动刷新，选择历史步骤后暂停跟随；主意识后续的生成稿、工具活动与实际发送另列。新调用记录实际步骤时间，旧调用只按已有记录还原顺序。轨迹记录不增加模型调用，也不参与记忆判断。

- **运行与调用**：查看当前开关、真实库存、滚动 24 小时用量，以及最近调用的耗时、背景、工具过程和模型上下文。调用记录从支持记录的版本开始保存，旧版本没有留下的过程会明确显示缺失。
- **记忆与连接**：搜索经历、人物与事实、主题和语义关联；查看实际保存的连接图，并从记忆条目回到原始消息。
- **账户与别名**：查看历史称呼和缓存的群成员资料，为已记录账户补充明确的别名。
- **原始消息**：检索群友发言、机器人实际发送以及单独标注的生成与工具活动，查看引用结构和前后文。
- **整理新消息**：手动处理此群待整理内容，跳过消息积累与工作时段等待；仍遵守后台开关、启用群范围与配置额度。
- **清理主会话上下文**：在“运行与调用”页手动为所选群开始新会话，可用于尝试恢复持续异常的主模型回复。原主会话及其中的文本、图片、工具记录完整保留，新会话继承原人格设置；MR 的记忆、短期笔记与群聊原文不作更改。正在返回的旧请求仍记入旧会话，后续请求使用新会话。当前会话已空或尚无会话时不重复创建。此操作不调用模型、不发送群消息，也不重启服务；是否恢复需看下一次实际回复。

模型与运行参数统一使用 AstrBot 自带配置面板。插件更新通过热重载完成，卸载时释放本实例的接口、观察器和正在处理的工作，不需要重启 AstrBot。

## 开发

`main.py` 接入 AstrBot，`mr_memory/` 提供存储、模型循环、向量检索、配置兼容与控制台接口，`pages/console/` 是对应页面。测试只使用合成内容；真实互动的检查和费用记录保存在仓库外的私有工作目录。局部测试通过不代表群聊效果已达标。
