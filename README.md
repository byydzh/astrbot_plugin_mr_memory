# MR Memory

MR 为 AstrBot 提供基于群聊经历的语义背景。它保存群友发言、机器人回应与可观察的工具活动，由原生 DeepSeek V4 Flash 主动查找相关事件、人物和关系，再把理解交给 AstrBot 的主回复模型。网络搜索、绘图等外部能力由其他插件负责。

## 工作方式

近期对话、上次工作记忆与少量语义候选进入同一个潜意识模型上下文。模型可以查原始消息、连续上下文、事件、人物、关系图和活动统计，根据读到的材料继续搜索或结束。结果是自然语言背景；没有前置语义审批、逐事实证书或第二套审查模型。

后台按小批次从新消息形成事件、语义与关联记忆。用户纠正、机器人实际回应和工具结果也作为经历参与理解。工作记忆随调用更新，长期记忆留在 SQLite；未完成的构建和索引工作仍可继续处理。

这种主动搜索沿用 [MRAgent 论文](https://arxiv.org/html/2606.06036v1) 的“根据中间证据调整下一步访问”机制。群聊接入、跨请求工作记忆和后台更新属于本插件的产品实现；不声称复现论文基准分数。

## 配置和数据

在 AstrBot 配置原生 DeepSeek V4 Flash Provider，将其 ID 填入 `subconscious_provider_id`。主回复模型及工具集保持 AstrBot 自身配置。默认搜索预算为 20 秒、最多 4 次模型调用；预算耗尽会明确标记未完成，不能视作没有记忆。

后台每批最多整理 60 条消息，每群每天默认 50 万 Token，达到预算后次日继续。前台搜索独立运行。状态命令显示后台用量；失去响应的调用暂按输入字节数和最大输出估算，不把未知费用记成零。

本地语义检索使用 `microsoft/harrier-oss-v1-270m`。首次安装需要下载公开模型并预热；后续使用本地缓存。源码仓库不包含群聊语料、配置凭据、模型文件或运行报告。

数据库沿用 `data/plugin_data/astrbot_plugin_mr_memory/scopes/`，已有群库、原文和图数据保留。每个群独立存储；旧处理器、证书、控制台及实验脚本已从代码移除。

`/mrmem` 查看本群最近一次潜意识运行状态。`/mrforget` 删除并停止记录自己的本群个人记忆。

## 开发

运行时只有 `main.py` 和 `mr_memory/` 中实际被调用的存储、模型循环、向量检索模块。测试只使用合成内容；真实互动的检查和费用记录保存在仓库外的私有工作目录。局部测试通过不代表群聊效果已达标。
